'use client';

/**
 * Card exports a shared layout primitive for grouped content.
 * Re-exports the richer Card implementation and subcomponents from the UI module.
 */
export { Card, CardHeader, CardTitle, CardDescription, CardContent } from '@/components/ui/Card';

