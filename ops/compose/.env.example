# =============================================================================
# DIRECTUS CMS - DOCKER COMPOSE ENVIRONMENT
# =============================================================================
# This is an example file showing required environment variables.
# DO NOT use these values in production!
#
# To retrieve production secrets from 1Password:
#   op document get "production.env" --vault "geralddagher-production" --output .env
# =============================================================================

# Database Configuration
DB_USER=directus
DB_PASSWORD=<secure-password>
DB_DATABASE=directus

# Directus Keys (generate with: openssl rand -hex 32)
KEY=<directus-key-uuid>
SECRET=<directus-secret-key>

# Admin Account
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=<secure-password>

# Cloudflare R2 Storage
R2_ACCESS_KEY=<r2-access-key>
R2_SECRET_KEY=<r2-secret-key>
R2_BUCKET=cms-assets
R2_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com

# Domain Configuration
CMS_DOMAIN=https://cms.example.com
CORS_ORIGIN=https://example.com

# Cache Configuration (optional)
CACHE_ENABLED=false
CACHE_STORE=memory
CACHE_REDIS=redis://redis:6379
